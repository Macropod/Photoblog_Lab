<div class="row">
  <div class="col-md-3">
    <div class="well sidebar-nav gallery-sidebar">
      <%= render 'shared/galleries' %>
    </div>
  </div>
  
  <div class="col-md-8">
    <% if @posts.any? && signed_in? %>
      <ol class="posts">
        <%= render @posts %>
      </ol>
      <%= will_paginate @posts %>
    <% end %>
  </div>

  <div class="col-md-1">
  </div>
</div>